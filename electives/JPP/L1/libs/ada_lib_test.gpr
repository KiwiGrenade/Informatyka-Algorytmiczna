project Ada_Lib_Test is
   for Source_Dirs use (".");  -- Assuming the Ada source files are in the current directory
   for Object_Dir use "../build/obj";   -- Directory where object files will be stored
   for Exec_Dir use "../build/bin";     -- Directory where executable files will be stored

   for Main use ("main.adb");  -- Main source file containing the test procedures

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat12");  -- Adjust compiler switches as needed
   end Compiler;
end Ada_Lib_Test;
